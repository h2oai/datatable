
.. xfunction:: datatable.math.square
    :src: src/core/expr/funary/exponential.cc resolve_op_square
    :cvar: doc_math_square
    :signature: square(x)

    The square of x, same as ``x ** 2.0``. As with all other math
    functions, the result is floating-point, even if the argument
    x is integer.
